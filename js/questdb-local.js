/* Questie Compass — local Questie database reader (placeholder; real reader lands in Phase 5) */
(function (root) {
  'use strict';
  root.QC = Object.assign(root.QC || {}, { LocalDB: { load: async () => null } });
})(window);
